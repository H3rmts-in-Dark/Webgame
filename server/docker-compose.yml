version: '3'

services:
  server-db:
    image: mariadb:10.7
    ports:
      - "3308:3306"
    volumes:
      - db-data:/var/lib/mysql
    environment:
      MARIADB_ROOT_PASSWORD: "root"
      
volumes:
  db-data: